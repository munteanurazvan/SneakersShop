{% block body %}
    <div class="container-fluid bg-info mb-2">
        <div class="row">
            <div class="col-12">
                <div class="container">
                    <div class="row">
                        <div class="col-6">
                            <a class="navbar-brand" href="{{ url('homepage') }}">
                                <img width="200px" src="{{ asset('images/logo_shop.jpg') }}" alt="logo.png">
                            </a>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-12 bg-info pb-1">
                            <nav class="navbar navbar-expand-lg navbar-light p-0 mb-1">
                                <nav class="collapse navbar-collapse" id="navbarSupportedContent">
                                    <ul class="navbar-nav">
                                        <li class="nav-item dropdown">
                                            <div class="dropdown mr-1">
                                                <button class="btn dropdown-toggle" type="button"
                                                        id="dropdownMenuButton" data-toggle="dropdown"
                                                        aria-haspopup="true"
                                                        aria-expanded="false">
                                                    CATEGORIE
                                                </button>
                                                <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                                                    {% for category in categories %}
                                                        <a class="dropdown-item"
                                                           href="{{ url('category', {'category':category.id}) }}">
                                                            {{ category.name }}
                                                        </a>
                                                    {% endfor %}
                                                </div>
                                            </div>
                                        </li>
                                        <li>
                                            <a class="btn btn-light mr-1" href="{{ url('homepage') }}" role="button">Home <i class="fa fa-home" aria-hidden="true"></i></a>
                                        </li>
                                        <li>
                                            <a class="btn btn-light mr-1" href="{{ url('contact') }}" role="button">Contact <i class="fa fa-envelope" aria-hidden="true"></i></a>
                                        </li>
                                    </ul>
                                </nav>
                                        <nav class="navbar navbar-expand-lg ml-auto align-content-end p-0">
                                            <ul class="navbar-nav">
                                                <li>
                                                    <a class="btn btn-light mr-1" href="{{ url('cart') }}" role="button">Cosul meu <i class="fa fa-shopping-cart" aria-hidden="true"></i>
                                                        {% if cartService.getCartCount() > 0 %}
                                                            <span class="badge badge-danger">{{ cartService.getCartCount() }}</span>
                                                        {% endif %}
                                                    </a>
                                                </li>
                                                <li class="nav-item ">
                                                    {% if app.user %}
                                                    <button class="btn btn-light">Esti logat ca {{ app.user.username }}, <a href="{{ path('app_logout') }}">Logout</a></button>
                                                </li>
                                                {% else %}
                                                    <li class="nav-item">
                                                        <a class="btn btn-light" href="{{ url('app_login') }}" role="button">Login <i class="fa fa-sign-in" aria-hidden="true"></i></a>
                                                    </li>
                                                {% endif %}
                                            </ul>
                                        </nav>
                                </nav>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="container">
        <img class="align-content-center" width="550px" height="150px" src="{{ asset('images/10off.jpg') }}" alt="10off.jpg"/><img width="550px" height="150px" src="{{ asset('images/10off.jpg') }}" alt="10off.jpg"/>
    </div>
{% endblock %}