{% extends 'base.html.twig' %}

{% block title %}Hello DefaultController!{% endblock %}

{% block body %}

<div class="container">
    <div class="row">
        <div class="col-12 mt-2">
            <h3>{{ category.name }}</h3>
        </div>
        {% for product in category.getProducts() %}
            {{ include ('productCard/productCard.html.twig') }}
        {% endfor %}
    </div>
</div>
{% endblock %}